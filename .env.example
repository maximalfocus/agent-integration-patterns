# OpenAI API Key for the Agent
OPENAI_API_KEY=sk-...

# GitHub Personal Access Token (Classic or Fine-grained)
# Required for higher rate limits and reading repo data
# Select scope: public_repo (read-only access to public info is enough)
GITHUB_TOKEN=ghp_...
